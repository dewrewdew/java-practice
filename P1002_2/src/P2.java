public class P2 {
    public static void main(String[] args) {
            // 타입 : 기본타입(8가지), 참조타입 => 타입은 이 2개가 끝!! 예외 없음!!
            // stack : 지역변수, 스코프의 영향을 받음
            // heap : new로 생성되는 메모리, 그 메모리를 참조하는 값이 없으면 GC(garbage Collector)가 수거해감
    }
}

